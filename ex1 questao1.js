let numbers = [10,20,30,40,50] //criando um array
numbers.push(60) // adicionando o numero 60 no final do array
numbers.pop(60) // removendo o numero 60 do array
let localizanumber=numbers.indexOf(30)// localizar o local do dado dentro do array
console.log(localizanumber) // imprimir na tela a localização
console.log(numbers) // imprimir na tela o array